coverage:
  status:
    project:
      default:
        informational: true
        if_no_uploads: success
    patch:
      default:
        informational: true
        if_no_uploads: success

ignore:
  - "src/remote_debugging/offsets/v_*.rs"
  - "tests/"

flag_management:
  default_rules:
    carryforward: true
